apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-service
  namespace: auralux
  labels: { app: music-service, tier: backend }
spec:
  replicas: 3
  selector:
    matchLabels: { app: music-service }
  template:
    metadata:
      labels: { app: music-service, tier: backend }
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: music-service
          image: auralux/music-service:latest
          ports: [{ containerPort: 3003, name: http }]
          envFrom:
            - configMapRef: { name: auralux-config }
            - secretRef: { name: auralux-secrets }
          env:
            - { name: PORT, value: "3003" }
            - { name: MONGO_URI, value: "mongodb://mongodb-0.mongodb-headless.auralux.svc.cluster.local:27017/auralux_music" }
          resources:
            requests: { cpu: 200m, memory: 256Mi }
            limits: { cpu: 1000m, memory: 1Gi }
          livenessProbe:
            httpGet: { path: /health/live, port: http }
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: music-service
  namespace: auralux
spec:
  type: ClusterIP
  selector: { app: music-service }
  ports: [{ port: 3003, targetPort: http, name: http }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: music-service-hpa
  namespace: auralux
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: music-service }
  minReplicas: 3
  maxReplicas: 15
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 65 } }
